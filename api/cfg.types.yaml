package: api                        # имя Go-пакета в сгенерированном файле
generate:
  models: true                      # генерировать типы/структуры
output: api/types.gen.go            # куда положить результат
# .gen.go — конвенция: суффикс говорит что файл сгенерирован, не трогай руками